# 最终呈现
用户输入文本, 我可以给他返回多模态的数据
```
上层(用户/LLM)输入: 给我明天北京的天气情况
输出: 一张天气预报的图, 以及文字讲解, 还有时间信息 
```
- 这是接入数据库的一个智能体, 也就是分解上层的文本需求, 转换成多个`SQL`语句查询不同模态的数据库
- 查询的前提是数据库里面已经有了这些数据, 也就是这个是数据库上层的一个`agent`


# 项目 TODO
- `.env`里面的各种配置: 模型, 模型`api`, 关系数据库, 向量数据库等都是硬编码的, 改成引擎加载时由用户进行选择